# Kubernetes Skill（第一期占位）

本目录为 **第一期征集** 占位：请 **复制到仓库根目录** 后再编写完整内容。

```bash
cp -R wanted-phase1/k8s_skill ./k8s_skill
```

## 请你补充（建议）

- 你最常处理的 3～5 类场景（如 Pod Pending、网络不通、存储挂载失败等）；
- 每类场景的 **安全排查顺序**、常用命令与 **禁止在生产自动执行** 的操作；
- 可选：`references/` 放冗长手册节选，`resource/` 放示例 YAML 片段。

提交 PR 前在 `mate.json` 填写 **author** 与 **affiliation**。
