# Prometheus Skill（第一期占位）

本目录为 **第一期征集** 占位：请 **复制到仓库根目录** 后再编写完整内容。

```bash
cp -R wanted-phase1/prometheus_skill ./prometheus_skill
```

## 请你补充（建议）

- PromQL 编写范式、Recording rule 与告警噪声治理；
- 联邦、远程存储或高可用部署中的踩坑点；
- 与 Alertmanager、Grafana 联动的常见故障定位流程。

提交 PR 前在 `mate.json` 填写 **author** 与 **affiliation**。
