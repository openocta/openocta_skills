# Zabbix Skill（第一期占位）

本目录为 **第一期征集** 占位：请 **复制到仓库根目录** 后再编写完整内容。

```bash
cp -R wanted-phase1/zabbix_skill ./zabbix_skill
```

## 请你补充（建议）

- 模板与 LLD、触发器依赖与告警降噪；
- Proxy / Server 瓶颈与数据库维护；
- 升级与迁移的检查清单。

提交 PR 前在 `mate.json` 填写 **author** 与 **affiliation**。
